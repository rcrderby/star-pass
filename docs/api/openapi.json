{
  "components": {
    "schemas": {
      "BlockerView": {
        "description": "One reason one event cannot become a shift. ",
        "properties": {
          "eventId": {
            "description": "Event that cannot be sent.",
            "title": "Eventid",
            "type": "string"
          },
          "reason": {
            "description": "Why: no_opportunity, ends_before_start, no_slots. An event with two things wrong with it appears once for each, so fixing one does not reveal another.",
            "title": "Reason",
            "type": "string"
          }
        },
        "required": [
          "eventId",
          "reason"
        ],
        "title": "BlockerView",
        "type": "object"
      },
      "CollectRequest": {
        "description": "Which calendar to collect, and over which days. ",
        "properties": {
          "calendar": {
            "description": "Which configured calendar to read. One the deployment has configured; the names are not part of this contract, because they are a property of a deployment rather than of the API. A name that is not configured is refused, and the refusal lists the ones that are.",
            "examples": [
              "events"
            ],
            "title": "Calendar",
            "type": "string"
          },
          "window": {
            "$ref": "#/components/schemas/WindowRequest",
            "description": "The days to cover."
          }
        },
        "required": [
          "calendar",
          "window"
        ],
        "title": "CollectRequest",
        "type": "object"
      },
      "EventRoleView": {
        "description": "One opportunity an event creates a shift for. ",
        "properties": {
          "edited": {
            "description": "Whether a person changed the count from the opportunity's default. Recorded rather than compared against the default now, because the default can change.",
            "title": "Edited",
            "type": "boolean"
          },
          "needId": {
            "description": "Amplify need ID the shift is created under.",
            "title": "Needid",
            "type": "string"
          },
          "slots": {
            "description": "Volunteers wanted.",
            "title": "Slots",
            "type": "integer"
          }
        },
        "required": [
          "needId",
          "slots",
          "edited"
        ],
        "title": "EventRoleView",
        "type": "object"
      },
      "EventView": {
        "description": "One calendar event, as the current revision holds it. ",
        "properties": {
          "addedByHand": {
            "description": "Whether a person pulled the event in rather than the search finding it.",
            "title": "Addedbyhand",
            "type": "boolean"
          },
          "blocking": {
            "description": "Whether this event stops the run being sent. True when it has no opportunity to create a shift under. The run stops rather than dropping it, because a missing shift is invisible until volunteers cannot sign up.",
            "title": "Blocking",
            "type": "boolean"
          },
          "calendarEnd": {
            "description": "End time on the calendar, in the run's zone.",
            "title": "Calendarend",
            "type": "string"
          },
          "calendarStart": {
            "description": "Start time on the calendar, in the run's zone.",
            "title": "Calendarstart",
            "type": "string"
          },
          "cappedAt": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "description": "The opportunity maximum that shortened this shift, or null when nothing did. Present so a reader is told which number decided the length rather than left wondering why the shift is shorter than the event.",
            "title": "Cappedat"
          },
          "category": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Data model category the title matched, or null when nothing matched.",
            "title": "Category"
          },
          "date": {
            "description": "Day of the event, as an ISO date.",
            "title": "Date",
            "type": "string"
          },
          "duplicateOf": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Identifier of an earlier event in this revision that would create the same shift, or null when none would. Sameness is the row Amplify receives -- need ID, date, start and end -- so two events at one hour under different opportunities are two shifts, not a repeat.",
            "title": "Duplicateof"
          },
          "id": {
            "description": "Identifier, unique within the revision.",
            "title": "Id",
            "type": "string"
          },
          "lengthMinutes": {
            "description": "How long the shift lasts, which is the duration Amplify is given. Not above zero means the shift ends before it starts, which blocks the send.",
            "title": "Lengthminutes",
            "type": "integer"
          },
          "match": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/MatchView"
              },
              {
                "type": "null"
              }
            ],
            "description": "How the category was reached, or null when the event was added by hand or matched nothing. Stored as it happened, because the data model can change between the day a run is collected and the day it is read."
          },
          "roles": {
            "description": "The opportunities this event creates shifts for.",
            "items": {
              "$ref": "#/components/schemas/EventRoleView"
            },
            "title": "Roles",
            "type": "array"
          },
          "shiftEnd": {
            "description": "End time of the shift to create.",
            "title": "Shiftend",
            "type": "string"
          },
          "shiftStart": {
            "description": "Start time of the shift to create, which is the calendar time plus the opportunity's offset.",
            "title": "Shiftstart",
            "type": "string"
          },
          "title": {
            "description": "Event title, as the calendar gave it.",
            "title": "Title",
            "type": "string"
          }
        },
        "required": [
          "id",
          "title",
          "date",
          "calendarStart",
          "calendarEnd",
          "shiftStart",
          "shiftEnd",
          "lengthMinutes",
          "addedByHand",
          "roles",
          "blocking"
        ],
        "title": "EventView",
        "type": "object"
      },
      "Health": {
        "description": "The service's answer about itself. ",
        "properties": {
          "status": {
            "description": "Always \"ok\": a reply at all is the health report.",
            "examples": [
              "ok"
            ],
            "title": "Status",
            "type": "string"
          }
        },
        "required": [
          "status"
        ],
        "title": "Health",
        "type": "object"
      },
      "JobView": {
        "description": "A long operation, as a caller sees it. ",
        "properties": {
          "createdAt": {
            "description": "When it was asked for, as an ISO-8601 UTC time.",
            "title": "Createdat",
            "type": "string"
          },
          "detail": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Why it failed, when it did and when the reason is one a caller can act on. A failure the service did not expect says so without its reason, which is in the service log against the job.",
            "title": "Detail"
          },
          "finishedAt": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "When it stopped, or null while it has not.",
            "title": "Finishedat"
          },
          "id": {
            "description": "Identifier the job is addressed by.",
            "title": "Id",
            "type": "string"
          },
          "kind": {
            "description": "What it is doing: collect, recollect, send.",
            "title": "Kind",
            "type": "string"
          },
          "runId": {
            "description": "Run the job is working on.",
            "title": "Runid",
            "type": "string"
          },
          "startedAt": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "When it began, or null while it is queued.",
            "title": "Startedat"
          },
          "status": {
            "description": "Where it is: queued, running, succeeded, failed, interrupted. \"interrupted\" means the service stopped while it was in hand, so how far it got is unknown; resuming one is a deliberate action, never automatic.",
            "title": "Status",
            "type": "string"
          }
        },
        "required": [
          "id",
          "runId",
          "kind",
          "status",
          "createdAt"
        ],
        "title": "JobView",
        "type": "object"
      },
      "LogEntryView": {
        "description": "One line of a run's change log. ",
        "properties": {
          "entry": {
            "description": "What changed, written for a reader.",
            "title": "Entry",
            "type": "string"
          },
          "id": {
            "description": "Identifier, ascending in the order entries were written.",
            "title": "Id",
            "type": "integer"
          },
          "loggedAt": {
            "description": "When the change was made, as an ISO-8601 UTC time.",
            "title": "Loggedat",
            "type": "string"
          },
          "principalId": {
            "description": "Who made it. One value while the credential is a static token, and a real subject once an identity provider issues one, with no change to what records it.",
            "title": "Principalid",
            "type": "string"
          },
          "revision": {
            "description": "Revision that was current when the change was made.",
            "title": "Revision",
            "type": "integer"
          }
        },
        "required": [
          "id",
          "revision",
          "loggedAt",
          "principalId",
          "entry"
        ],
        "title": "LogEntryView",
        "type": "object"
      },
      "MatchView": {
        "description": "How an event's title was matched to a category. ",
        "properties": {
          "keyword": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "The alias that matched, or null for a fuzzy match.",
            "title": "Keyword"
          },
          "kind": {
            "description": "How it matched: \"keyword\" for an alias whose words all appear in the title, or \"fuzzy\" for the fallback.",
            "title": "Kind",
            "type": "string"
          },
          "score": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "description": "Fuzzy confidence from 0 to 100, or null for a keyword match, which is not scored.",
            "title": "Score"
          }
        },
        "required": [
          "kind"
        ],
        "title": "MatchView",
        "type": "object"
      },
      "OpportunityView": {
        "description": "An Amplify opportunity a run creates shifts under. ",
        "properties": {
          "defaultSlots": {
            "description": "Volunteers wanted per shift, before any edit.",
            "title": "Defaultslots",
            "type": "integer"
          },
          "maxLength": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "description": "Longest shift the opportunity accepts, in minutes, or null when it sets no maximum.",
            "title": "Maxlength"
          },
          "needId": {
            "description": "Amplify need ID.",
            "title": "Needid",
            "type": "string"
          },
          "offsetEnd": {
            "description": "Minutes added to an event's end to reach the shift's.",
            "title": "Offsetend",
            "type": "integer"
          },
          "offsetStart": {
            "description": "Minutes added to an event's start to reach the shift's.",
            "title": "Offsetstart",
            "type": "integer"
          },
          "title": {
            "description": "Amplify opportunity title, as displayed.",
            "title": "Title",
            "type": "string"
          },
          "url": {
            "description": "Public address of the opportunity.",
            "title": "Url",
            "type": "string"
          }
        },
        "required": [
          "needId",
          "title",
          "url",
          "offsetStart",
          "offsetEnd",
          "defaultSlots"
        ],
        "title": "OpportunityView",
        "type": "object"
      },
      "PreviewRowView": {
        "description": "What one Amplify opportunity would receive. ",
        "properties": {
          "alreadyInAmplify": {
            "description": "Shifts this opportunity is asked for that it already holds, and which a send would skip. A row where this is the whole ask and `willCreate` is zero is an opportunity a send has nothing left to do for.",
            "title": "Alreadyinamplify",
            "type": "integer"
          },
          "firstDate": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Earliest day a shift would be created on, or null when none would be.",
            "title": "Firstdate"
          },
          "lastDate": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Latest day a shift would be created on, or null when none would be. Of the shifts that would be created, not of every event under this opportunity: these are the days about to arrive in Amplify.",
            "title": "Lastdate"
          },
          "needId": {
            "description": "Amplify need ID the shifts would be created under.",
            "title": "Needid",
            "type": "string"
          },
          "slots": {
            "description": "Volunteers wanted across the shifts that would be created. A skipped shift asks for nobody: it exists already, wanting whatever it was created wanting.",
            "title": "Slots",
            "type": "integer"
          },
          "title": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "The opportunity's title, or null when the run stored no opportunity for this need ID, which means collection did not resolve one.",
            "title": "Title"
          },
          "willCreate": {
            "description": "Shifts this opportunity would receive, without the ones it already holds.",
            "title": "Willcreate",
            "type": "integer"
          }
        },
        "required": [
          "needId",
          "willCreate",
          "alreadyInAmplify",
          "slots"
        ],
        "title": "PreviewRowView",
        "type": "object"
      },
      "PreviewTotalsView": {
        "description": "What a send would do, totalled over the whole revision. ",
        "properties": {
          "alreadyInAmplify": {
            "description": "Shifts the revision asks for that Amplify already has, read live from the opportunities themselves rather than from any record of what this run sent. They are skipped rather than created again; `skipped` names them.",
            "title": "Alreadyinamplify",
            "type": "integer"
          },
          "blockingEvents": {
            "description": "Events that cannot be sent. Above zero means nothing can be sent at all: the run stops and names them rather than dropping them, because a missing shift is invisible until volunteers cannot sign up.",
            "title": "Blockingevents",
            "type": "integer"
          },
          "repeatedRows": {
            "description": "How many shifts the revision asks for more than once. They create one shift, not several; the figure is here so a reader is told rather than left to wonder why the total is below the number of rows they can see.",
            "title": "Repeatedrows",
            "type": "integer"
          },
          "willCreate": {
            "description": "Shifts that would be created. Counted by identity -- need ID, date, start and end -- so two events asking for the same row count once, and without the ones Amplify already holds. This is the number of rows that will arrive, and the number a send confirms against.",
            "title": "Willcreate",
            "type": "integer"
          }
        },
        "required": [
          "willCreate",
          "alreadyInAmplify",
          "repeatedRows",
          "blockingEvents"
        ],
        "title": "PreviewTotalsView",
        "type": "object"
      },
      "PreviewView": {
        "description": "What sending the current revision would create.\n\nGrouped by opportunity and never by category: several\ncategories share one Amplify listing, so grouping by category\nwould show that listing twice under two names and split a\ntotal the reader is about to check against Amplify.\n\nEvery opportunity the revision touches is read live while this\nis answered, so the totals are net of what Amplify already\nholds. The send re-reads the same way inside its own\ntransaction, which is what makes the number shown here the\nnumber of rows that arrive.",
        "properties": {
          "blockers": {
            "description": "Every reason an event cannot be sent, in the order the events are shown.",
            "items": {
              "$ref": "#/components/schemas/BlockerView"
            },
            "title": "Blockers",
            "type": "array"
          },
          "rows": {
            "description": "One per opportunity the revision asks for a shift under, by need ID. An opportunity the run resolved but asks nothing of has no row; one whose shifts Amplify already holds keeps its row and says so.",
            "items": {
              "$ref": "#/components/schemas/PreviewRowView"
            },
            "title": "Rows",
            "type": "array"
          },
          "skipped": {
            "description": "Every shift Amplify already has, by need ID and then by when it falls. A send skips exactly these.",
            "items": {
              "$ref": "#/components/schemas/SkippedShiftView"
            },
            "title": "Skipped",
            "type": "array"
          },
          "totals": {
            "$ref": "#/components/schemas/PreviewTotalsView",
            "description": "What a send would do, totalled over the whole revision rather than per opportunity."
          }
        },
        "required": [
          "totals",
          "rows",
          "skipped",
          "blockers"
        ],
        "title": "PreviewView",
        "type": "object"
      },
      "RecollectRequest": {
        "description": "What the operator was shown before asking to collect again. ",
        "properties": {
          "expectedChangeCount": {
            "description": "How many changes the operator was told a recollection would discard. Collecting again replaces the run's events with what the calendar has now, so any editing done since it was collected is left behind in the revision it was done in.\n\nThe service refuses when this does not match what the run actually holds. That is the stale-tab case: a number read from a page somebody left open describes a run that has moved on, and a confirmation dialog cannot tell the difference. Read the run again and ask again with what it says now.",
            "examples": [
              0
            ],
            "minimum": 0.0,
            "title": "Expectedchangecount",
            "type": "integer"
          }
        },
        "required": [
          "expectedChangeCount"
        ],
        "title": "RecollectRequest",
        "type": "object"
      },
      "RevisionView": {
        "description": "One numbered version of a run's events. ",
        "properties": {
          "changes": {
            "description": "How many changes were made while this revision was the current one. Zero means it was sealed and left, which is what tells a reader which revision in a list is worth looking at.",
            "title": "Changes",
            "type": "integer"
          },
          "createdAt": {
            "description": "When it was created, as an ISO-8601 UTC time.",
            "title": "Createdat",
            "type": "string"
          },
          "current": {
            "description": "Whether this is the revision being edited now. Exactly one revision of a run is current, and it is the last: everything below it is history and is never written to again.",
            "title": "Current",
            "type": "boolean"
          },
          "label": {
            "description": "How the revision is named to a reader.",
            "title": "Label",
            "type": "string"
          },
          "number": {
            "description": "Position in the run's history, from one.",
            "title": "Number",
            "type": "integer"
          }
        },
        "required": [
          "number",
          "createdAt",
          "label",
          "changes",
          "current"
        ],
        "title": "RevisionView",
        "type": "object"
      },
      "RunCountsView": {
        "description": "What the run's current revision holds. ",
        "properties": {
          "events": {
            "description": "Events in the current revision.",
            "title": "Events",
            "type": "integer"
          },
          "shifts": {
            "description": "Shifts a send would create. Counted per role rather than per event, because an event serving both skating and non-skating officials creates two.",
            "title": "Shifts",
            "type": "integer"
          },
          "unmatched": {
            "description": "Events that would create no shift at all, and so stop the run being sent. A title that matched nothing has no opportunity to create a shift under, and a category that resolved to no need ID leaves the same absence.",
            "title": "Unmatched",
            "type": "integer"
          }
        },
        "required": [
          "events",
          "shifts",
          "unmatched"
        ],
        "title": "RunCountsView",
        "type": "object"
      },
      "RunDetailView": {
        "description": "A run with everything the review screen reads at once.\n\nThe events, the opportunities labelling them and the change\nlog arrive together because a screen showing one shows all\nthree, and reading them separately would let them disagree:\neach read would see the state at a different moment.",
        "properties": {
          "activeJobId": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Identifier of the job still working on this run, or null when nothing is. What makes a run somebody walked away from reattachable: read the job, or follow its events.",
            "title": "Activejobid"
          },
          "calendar": {
            "description": "Which configured calendar was collected.",
            "title": "Calendar",
            "type": "string"
          },
          "collectedAt": {
            "description": "When it was created, as an ISO-8601 UTC time.",
            "title": "Collectedat",
            "type": "string"
          },
          "counts": {
            "$ref": "#/components/schemas/RunCountsView",
            "description": "What the current revision holds."
          },
          "currentRevision": {
            "description": "Number of the revision now being edited, or 0 before the first one exists.",
            "title": "Currentrevision",
            "type": "integer"
          },
          "events": {
            "description": "The current revision's events, by date and start time.",
            "items": {
              "$ref": "#/components/schemas/EventView"
            },
            "title": "Events",
            "type": "array"
          },
          "id": {
            "description": "Identifier the run is addressed by. Minted by the server, and never the path of a file the run produced.",
            "title": "Id",
            "type": "string"
          },
          "log": {
            "description": "Every change made to the run, oldest first. Written where the change is made rather than assembled by whatever displays it, so it survives a reload and reads the same in a browser and a terminal.",
            "items": {
              "$ref": "#/components/schemas/LogEntryView"
            },
            "title": "Log",
            "type": "array"
          },
          "opportunities": {
            "description": "Amplify titles for every need ID this run touches, resolved while the run was collected and stored with it. Every review row is labelled with one, so they are not a lookup deferred to preview time.",
            "items": {
              "$ref": "#/components/schemas/OpportunityView"
            },
            "title": "Opportunities",
            "type": "array"
          },
          "revisedAt": {
            "description": "When it last changed, as an ISO-8601 UTC time. The time it was collected, for a run nothing has changed yet.",
            "title": "Revisedat",
            "type": "string"
          },
          "sentAt": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "When its shifts reached Amplify, or null while they have not.",
            "title": "Sentat"
          },
          "status": {
            "description": "Where the run is: collecting, unsent, partly_sent, sent.",
            "title": "Status",
            "type": "string"
          },
          "window": {
            "$ref": "#/components/schemas/WindowView",
            "description": "The days the run covers."
          }
        },
        "required": [
          "id",
          "calendar",
          "window",
          "status",
          "collectedAt",
          "revisedAt",
          "currentRevision",
          "counts",
          "events",
          "opportunities",
          "log"
        ],
        "title": "RunDetailView",
        "type": "object"
      },
      "RunView": {
        "description": "One collection of calendar events, as a caller sees it. ",
        "properties": {
          "activeJobId": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Identifier of the job still working on this run, or null when nothing is. What makes a run somebody walked away from reattachable: read the job, or follow its events.",
            "title": "Activejobid"
          },
          "calendar": {
            "description": "Which configured calendar was collected.",
            "title": "Calendar",
            "type": "string"
          },
          "collectedAt": {
            "description": "When it was created, as an ISO-8601 UTC time.",
            "title": "Collectedat",
            "type": "string"
          },
          "counts": {
            "$ref": "#/components/schemas/RunCountsView",
            "description": "What the current revision holds."
          },
          "currentRevision": {
            "description": "Number of the revision now being edited, or 0 before the first one exists.",
            "title": "Currentrevision",
            "type": "integer"
          },
          "id": {
            "description": "Identifier the run is addressed by. Minted by the server, and never the path of a file the run produced.",
            "title": "Id",
            "type": "string"
          },
          "revisedAt": {
            "description": "When it last changed, as an ISO-8601 UTC time. The time it was collected, for a run nothing has changed yet.",
            "title": "Revisedat",
            "type": "string"
          },
          "sentAt": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "When its shifts reached Amplify, or null while they have not.",
            "title": "Sentat"
          },
          "status": {
            "description": "Where the run is: collecting, unsent, partly_sent, sent.",
            "title": "Status",
            "type": "string"
          },
          "window": {
            "$ref": "#/components/schemas/WindowView",
            "description": "The days the run covers."
          }
        },
        "required": [
          "id",
          "calendar",
          "window",
          "status",
          "collectedAt",
          "revisedAt",
          "currentRevision",
          "counts"
        ],
        "title": "RunView",
        "type": "object"
      },
      "SendRequest": {
        "description": "What the operator was shown before asking to send. ",
        "properties": {
          "expectedShiftCount": {
            "description": "How many shifts the operator was told a send would create. This is the preview's `totals.willCreate`, which is net of what Amplify already holds -- the number of rows that will arrive, and so the number the confirmation restates.\n\nThe service refuses when this does not match what a send would create now. Two things move it: the run being edited, and Amplify itself gaining or losing a shift. Both mean the number somebody confirmed against described a moment that has passed. Read the preview again and ask again with what it says now.",
            "examples": [
              12
            ],
            "minimum": 0.0,
            "title": "Expectedshiftcount",
            "type": "integer"
          }
        },
        "required": [
          "expectedShiftCount"
        ],
        "title": "SendRequest",
        "type": "object"
      },
      "SkippedShiftView": {
        "description": "One shift the revision asks for that Amplify already has.\n\nNamed per shift and never only counted (D16). A count says how\nmany rows will not arrive; it does not say which, and the\nreader deciding whether that is right is deciding about\nparticular days and times.",
        "properties": {
          "date": {
            "description": "Day it falls on, as an ISO date.",
            "title": "Date",
            "type": "string"
          },
          "needId": {
            "description": "Opportunity the shift would have been created under.",
            "title": "Needid",
            "type": "string"
          },
          "shiftEnd": {
            "description": "Time of day it ends.",
            "title": "Shiftend",
            "type": "string"
          },
          "shiftStart": {
            "description": "Time of day it starts.",
            "title": "Shiftstart",
            "type": "string"
          }
        },
        "required": [
          "needId",
          "date",
          "shiftStart",
          "shiftEnd"
        ],
        "title": "SkippedShiftView",
        "type": "object"
      },
      "Version": {
        "description": "The running version of the application. ",
        "properties": {
          "version": {
            "description": "The star-pass release this service is running.",
            "examples": [
              "1.16.4"
            ],
            "title": "Version",
            "type": "string"
          }
        },
        "required": [
          "version"
        ],
        "title": "Version",
        "type": "object"
      },
      "WindowRequest": {
        "description": "The days a collection is asked to cover.\n\nNo zone, unlike the window a run answers with.  The server's\nzone is the authoritative one, and a client that sent its own\nwould be deciding which days a run covers from wherever the\nperson happened to be sitting (D16).",
        "properties": {
          "end": {
            "description": "Day after the last day to cover, as an ISO date. Exclusive, so a one-day window is two consecutive dates. There is no limit on how long a window may be.",
            "examples": [
              "2026-10-01"
            ],
            "title": "End",
            "type": "string"
          },
          "start": {
            "description": "First day to cover, as an ISO date. Read in the server's time zone.",
            "examples": [
              "2026-09-01"
            ],
            "title": "Start",
            "type": "string"
          }
        },
        "required": [
          "start",
          "end"
        ],
        "title": "WindowRequest",
        "type": "object"
      },
      "WindowView": {
        "description": "The days a run covers, and the zone they are read in. ",
        "properties": {
          "end": {
            "description": "Day after the last day the run covers, as an ISO date. Exclusive, so a run covering one day carries two consecutive dates.",
            "title": "End",
            "type": "string"
          },
          "start": {
            "description": "First day the run covers, as an ISO date.",
            "title": "Start",
            "type": "string"
          },
          "timezone": {
            "description": "Zone the two dates are read in. The server's zone is the authoritative one: a client displays these dates and never works a window out in the zone of whoever is looking at it.",
            "title": "Timezone",
            "type": "string"
          }
        },
        "required": [
          "start",
          "end",
          "timezone"
        ],
        "title": "WindowView",
        "type": "object"
      }
    },
    "securitySchemes": {
      "Bearer token": {
        "description": "The value of STAR_PASS_API_TOKEN.",
        "scheme": "bearer",
        "type": "http"
      }
    }
  },
  "info": {
    "description": "The remote surface over the star-pass core. Everything the web interface can do is reachable here, and the command line client can do anything the web interface can.",
    "summary": "Bulk volunteer shift operations for the Rose City Rollers.",
    "title": "star-pass",
    "version": "1.16.4"
  },
  "openapi": "3.1.0",
  "paths": {
    "/v1/health": {
      "get": {
        "description": "Answers without authentication, for a container runtime or a proxy deciding whether to route to this process. It reports that the process is serving and does not check the database or any upstream service.",
        "operationId": "get_health",
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Health"
                }
              }
            },
            "description": "Successful Response"
          },
          "default": {
            "content": {
              "application/problem+json": {}
            },
            "description": "A problem document (RFC 9457)."
          }
        },
        "summary": "Report that the service is serving",
        "tags": [
          "service"
        ]
      }
    },
    "/v1/jobs/{job_id}": {
      "get": {
        "description": "Answers for a job whether it is queued, running or over. A caller watching one polls this, or reads the event stream for the same job to be told as it happens.",
        "operationId": "get_job",
        "parameters": [
          {
            "description": "Identifier the job was created with.",
            "in": "path",
            "name": "job_id",
            "required": true,
            "schema": {
              "description": "Identifier the job was created with.",
              "title": "Job Id",
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/JobView"
                }
              }
            },
            "description": "Successful Response"
          },
          "default": {
            "content": {
              "application/problem+json": {}
            },
            "description": "A problem document (RFC 9457)."
          }
        },
        "security": [
          {
            "Bearer token": [
              "runs:read"
            ]
          }
        ],
        "summary": "Report where a job has got to",
        "tags": [
          "jobs"
        ]
      }
    },
    "/v1/jobs/{job_id}/events": {
      "get": {
        "description": "A server-sent event stream. Each frame is named for what the job reported and carries what that report held. The stream ends with a \"job_finished\" frame when the job is over.\n\nIt is reattachable: a client that reconnects sends the identifier of the last frame it saw in \"Last-Event-ID\", and is given what it missed rather than what it already has. A browser sends that header itself.",
        "operationId": "stream_job_events",
        "parameters": [
          {
            "description": "Identifier the job was created with.",
            "in": "path",
            "name": "job_id",
            "required": true,
            "schema": {
              "description": "Identifier the job was created with.",
              "title": "Job Id",
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "text/event-stream": {}
            },
            "description": "The job's events, as they happen."
          },
          "default": {
            "content": {
              "application/problem+json": {}
            },
            "description": "A problem document (RFC 9457)."
          }
        },
        "security": [
          {
            "Bearer token": [
              "runs:read"
            ]
          }
        ],
        "summary": "Follow what a job reports, as it reports it",
        "tags": [
          "jobs"
        ]
      }
    },
    "/v1/jobs/{job_id}/resume": {
      "post": {
        "description": "Queues an interrupted job to be run again, and hands it back to a worker. A job left queued or running when the service stopped is marked interrupted at startup and never resumed on its own (D10): a send that resumed itself would write to a live volunteer system from state rebuilt after a crash.\n\nWhat runs is the ordinary work, pointed at a job that already exists. A resumed send reads every opportunity immediately before writing to it and creates exactly the rows the interrupted attempt did not, so resuming needs no record of how far that attempt got -- the question is answered by Amplify rather than by the job.\n\nThe job keeps its identifier and loses what the interrupted attempt recorded about itself: when it began, when it stopped, and why. Those described an attempt that is being made again, and leaving them would describe two runs of it at once.\n\nRefused for a job in any other state -- which is what stops a second click starting a second worker -- and while another job is working on the same run.",
        "operationId": "resume_job",
        "parameters": [
          {
            "description": "Identifier of the job to run again.",
            "in": "path",
            "name": "job_id",
            "required": true,
            "schema": {
              "description": "Identifier of the job to run again.",
              "title": "Job Id",
              "type": "string"
            }
          }
        ],
        "responses": {
          "202": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/JobView"
                }
              }
            },
            "description": "Successful Response"
          },
          "default": {
            "content": {
              "application/problem+json": {}
            },
            "description": "A problem document (RFC 9457)."
          }
        },
        "security": [
          {
            "Bearer token": [
              "runs:write"
            ]
          }
        ],
        "summary": "Run an interrupted job again",
        "tags": [
          "jobs"
        ]
      }
    },
    "/v1/runs": {
      "get": {
        "description": "Every run the service holds, most recently collected first, including one still being collected. Each carries what its current revision holds and the job still working on it, so the list is enough to decide what to open without reading each run in turn.",
        "operationId": "list_runs",
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "items": {
                    "$ref": "#/components/schemas/RunView"
                  },
                  "title": "Response List Runs",
                  "type": "array"
                }
              }
            },
            "description": "Successful Response"
          },
          "default": {
            "content": {
              "application/problem+json": {}
            },
            "description": "A problem document (RFC 9457)."
          }
        },
        "security": [
          {
            "Bearer token": [
              "runs:read"
            ]
          }
        ],
        "summary": "List the runs, newest first",
        "tags": [
          "runs"
        ]
      },
      "post": {
        "description": "Reads the calendar over the days given and stores what it finds as a new run, ready to review. The identifier is minted here and is never the path of a file.\n\nAnswers as soon as the run exists, with the job doing the work: reading a calendar and naming every opportunity it finds takes longer than a request should be held open. Read the job, or follow its events, to see how far it has got. The run is in the list from the moment this answers, so a person who closed the page can find it again.\n\nThe window is a first day and the day after the last, read in the server's time zone, and there is no limit on its length. An event that cannot become a correct shift does not stop the collection: it is stored, named as unmatched, and stops the **send** instead, so a reviewer sees everything the calendar held rather than a run with holes in it.",
        "operationId": "collect_run",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CollectRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "202": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/JobView"
                }
              }
            },
            "description": "Successful Response"
          },
          "default": {
            "content": {
              "application/problem+json": {}
            },
            "description": "A problem document (RFC 9457)."
          }
        },
        "security": [
          {
            "Bearer token": [
              "runs:write"
            ]
          }
        ],
        "summary": "Collect a calendar window into a new run",
        "tags": [
          "runs"
        ]
      }
    },
    "/v1/runs/{run_id}": {
      "get": {
        "description": "The run, the events of its current revision, the Amplify opportunities labelling them, and the log of everything done to it. Read together because a screen showing one shows all of them, and separate reads could disagree.\n\nEach event carries what its stored row does not say: how long the shift is, the maximum that shortened it, whether an earlier event would create the same shift, and whether it blocks the send.",
        "operationId": "get_run",
        "parameters": [
          {
            "description": "Identifier the run was created with.",
            "in": "path",
            "name": "run_id",
            "required": true,
            "schema": {
              "description": "Identifier the run was created with.",
              "title": "Run Id",
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/RunDetailView"
                }
              }
            },
            "description": "Successful Response"
          },
          "default": {
            "content": {
              "application/problem+json": {}
            },
            "description": "A problem document (RFC 9457)."
          }
        },
        "security": [
          {
            "Bearer token": [
              "runs:read"
            ]
          }
        ],
        "summary": "Read one run in full",
        "tags": [
          "runs"
        ]
      }
    },
    "/v1/runs/{run_id}/preview": {
      "get": {
        "description": "What a send would do, grouped by Amplify opportunity and never by category: several categories share one listing, so grouping by category would show it twice under two names and split a total the reader is about to check against Amplify.\n\nShifts are counted by identity -- need ID, date, start and end -- so two events asking for the same row count once, and a reader is told how many rows repeat rather than left to wonder why the total is below what they can see.\n\nAn event that cannot become a shift stops the whole send and is named with every reason it cannot, so fixing one does not reveal another.\n\nEvery opportunity the revision touches is read from Amplify while this is answered, so a shift that is already there is reported as skipped rather than counted in what would be created. The send asks the same question again inside its own transaction, which is what makes the total shown here the number of rows that arrive.",
        "operationId": "get_preview",
        "parameters": [
          {
            "description": "Identifier the run was created with.",
            "in": "path",
            "name": "run_id",
            "required": true,
            "schema": {
              "description": "Identifier the run was created with.",
              "title": "Run Id",
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PreviewView"
                }
              }
            },
            "description": "Successful Response"
          },
          "default": {
            "content": {
              "application/problem+json": {}
            },
            "description": "A problem document (RFC 9457)."
          }
        },
        "security": [
          {
            "Bearer token": [
              "runs:read"
            ]
          }
        ],
        "summary": "Report what sending this run would create",
        "tags": [
          "runs"
        ]
      }
    },
    "/v1/runs/{run_id}/recollect": {
      "post": {
        "description": "Reads the same calendar over the same days and replaces what the run holds with what is there now. The run keeps its identifier and its window; a new revision holds the fresh events, and the revisions before it stay readable, so what was replaced is still there to look at.\n\n**Editing done since the run was collected is left behind.** That is what `expectedChangeCount` is for: the operator is shown how much would be discarded and sends the number back, and a number that no longer matches means they were looking at a page describing a run that has moved on.\n\nRefused while another job is working on the run, and refused for a run that has sent shifts, which Amplify cannot take back.",
        "operationId": "recollect_run",
        "parameters": [
          {
            "description": "Identifier the run was created with.",
            "in": "path",
            "name": "run_id",
            "required": true,
            "schema": {
              "description": "Identifier the run was created with.",
              "title": "Run Id",
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/RecollectRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "202": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/JobView"
                }
              }
            },
            "description": "Successful Response"
          },
          "default": {
            "content": {
              "application/problem+json": {}
            },
            "description": "A problem document (RFC 9457)."
          }
        },
        "security": [
          {
            "Bearer token": [
              "runs:write"
            ]
          }
        ],
        "summary": "Collect a run's calendar window again",
        "tags": [
          "runs"
        ]
      }
    },
    "/v1/runs/{run_id}/revisions": {
      "get": {
        "description": "Every numbered version of the run's events, in the order they were made. Everything below the current revision is history and is never written to again: editing adds a revision holding a copy, and reverting does the same rather than deleting anything, so the record of what was done survives being undone.\n\nEach carries how many changes were made while it was current, which is what says whether a revision is worth looking at.",
        "operationId": "list_revisions",
        "parameters": [
          {
            "description": "Identifier the run was created with.",
            "in": "path",
            "name": "run_id",
            "required": true,
            "schema": {
              "description": "Identifier the run was created with.",
              "title": "Run Id",
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "items": {
                    "$ref": "#/components/schemas/RevisionView"
                  },
                  "title": "Response List Revisions",
                  "type": "array"
                }
              }
            },
            "description": "Successful Response"
          },
          "default": {
            "content": {
              "application/problem+json": {}
            },
            "description": "A problem document (RFC 9457)."
          }
        },
        "security": [
          {
            "Bearer token": [
              "runs:read"
            ]
          }
        ],
        "summary": "List a run's revisions, oldest first",
        "tags": [
          "runs"
        ]
      }
    },
    "/v1/runs/{run_id}/send": {
      "post": {
        "description": "Creates every shift the run's current revision asks for that Amplify does not already have. **This is the one thing star-pass does that cannot be undone**: Amplify has no way to take a shift back.\n\nRequires an `Idempotency-Key` header. The key is stored with what this request answered, so a retry after a lost response is given the first answer rather than sending again. A key carrying a different `expectedShiftCount` is refused rather than answered from the first request, because a key is a promise that the request is the one already made.\n\nDuplicate safety does not rest on that key. Every opportunity is read from Amplify immediately before the request that writes to it, and a shift already there is skipped -- by row identity, never by a count, so nothing depends on knowing which shifts a previous attempt reached. The same read is made here, before the job is queued, and is what `expectedShiftCount` is checked against.\n\nAnswers as soon as the job exists. One request is sent per opportunity, carrying every shift it is missing; a batch is recorded as sent only once its request has succeeded, so a batch whose answer never arrived leaves the run `partly_sent` and the next send reads the opportunity and sends the difference.\n\nRefused while another job is working on the run, while the run is still being collected, and while any event in it cannot become a shift -- a missing shift is invisible until volunteers cannot sign up, so the run stops rather than sending the rest.",
        "operationId": "send_run",
        "parameters": [
          {
            "description": "Identifier the run was created with.",
            "in": "path",
            "name": "run_id",
            "required": true,
            "schema": {
              "description": "Identifier the run was created with.",
              "title": "Run Id",
              "type": "string"
            }
          },
          {
            "description": "A value of the caller's choosing, unique to this attempt to send this run. Repeat it when retrying a request whose answer was lost; choose a new one when asking for something different.",
            "in": "header",
            "name": "Idempotency-Key",
            "required": true,
            "schema": {
              "description": "A value of the caller's choosing, unique to this attempt to send this run. Repeat it when retrying a request whose answer was lost; choose a new one when asking for something different.",
              "minLength": 1,
              "title": "Idempotency-Key",
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/SendRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "202": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/JobView"
                }
              }
            },
            "description": "Successful Response"
          },
          "default": {
            "content": {
              "application/problem+json": {}
            },
            "description": "A problem document (RFC 9457)."
          }
        },
        "security": [
          {
            "Bearer token": [
              "send:execute"
            ]
          }
        ],
        "summary": "Create this run's shifts in Amplify",
        "tags": [
          "runs"
        ]
      }
    },
    "/v1/version": {
      "get": {
        "description": "Return the running version.\n\nArgs:\n    principal (Principal):\n        The authenticated caller, which the dependency supplies\n        after checking the scope.\n\nReturns:\n    version (Version):\n        The version of the application that is serving.",
        "operationId": "get_version",
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Version"
                }
              }
            },
            "description": "Successful Response"
          },
          "default": {
            "content": {
              "application/problem+json": {}
            },
            "description": "A problem document (RFC 9457)."
          }
        },
        "security": [
          {
            "Bearer token": [
              "config:read"
            ]
          }
        ],
        "summary": "Report the running version",
        "tags": [
          "service"
        ]
      }
    }
  }
}
