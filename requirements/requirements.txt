# Everything the container runs: the API service, the front end and
# the command line.
#
# What both this and the Slack image need is in the core file, which
# is the only place those packages are pinned.

-r requirements_core.txt

# API service
fastapi==0.141.1
uvicorn==0.52.3

# The front end's connection to the API service. Async, and it passes
# an event stream through as it arrives rather than at the end, which
# is what watching a job needs.
httpx2==2.12.0

# JSON Schema validation
jsonschema==4.26.0
