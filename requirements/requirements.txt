# Everything the container runs: the API service, the front end and
# the command line.
#
# Two lists, read as one.  The core file is what every way in needs
# and the whole of what the Slack image installs; the service file is
# what only the API service and the front end need.  Both are read by
# 'pyproject.toml' as well, which is what keeps the package's declared
# dependencies and the installed ones one list rather than two that
# can disagree.

-r requirements_core.txt
-r requirements_service.txt
